<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PermissionAI">

        <activity
            android:name="com.kva.permissionai.ui.activities.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.kva.permissionai.ui.activities.DashboardActivity"
            android:exported="false" />

        <activity
            android:name="com.kva.permissionai.ui.activities.MainActivity"
            android:exported="false"
            android:label="App Scanner" />

        <activity
            android:name="com.kva.permissionai.ui.activities.DetailActivity"
            android:exported="false"
            android:label="App Risk Analysis" />

    </application>

</manifest>